#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'

$LOAD_PATH.unshift(File.expand_path(File.join('..', 'lib'), __dir__))
require 'sql_to_ruby'

puts SQLToRuby.convert(ARGV[0])
